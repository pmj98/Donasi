

function index_donasi(a) {
  var r = ["bencana", "banjir", "gempa", "tsunami", "zakat", "masjid", "pesantren", "palestina", "mualaf", "islam"],
    e = 0;
  a: for (var n = 0; n < 100; ++n)
    for (var t = Math.floor(20 * Math.random()), i = a[t].judul.toLowerCase(), s = 0; s < r.length; ++s)
      if (-1 != i.search(r[s])) {
        e = t;
        break a
      }
  return e
}
$(document).ready(function() {
  $.ajax({
    method: "GET",
    url: "https://nugrohoprayogo.id/kitabisa/api.php",
    dataType: "json"
  }).done(function(a) {
    var r = index_donasi(a);
    $(".donasi a").attr("href", a[r].url), $(".gambar-donasi").css("background-image", "url(" + a[r].gambar + ")"), $(".judul-donasi").text(a[r].judul), $(".penyelenggara").text(a[r].pembuat), $(".penyelenggara-icon").css("background-image", "url(" + a[r].tag + ")"), 0 != a[r].progress ? $(".terkumpul").text(a[r].donasi_sekarang + " (" + a[r].progress + "% dari Target)") : $(".terkumpul").text(a[r].donasi_sekarang), 0 != a[r].sisa_hari ? $(".hari").text(a[r].sisa_hari + " Hari") : $(".hari").text("Tidak di Tentukan");
    $(".wrap-donasi").show();
  })
});
